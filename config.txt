CH2-03-13-2024-nodejs-express-json
1. create directory untuk project nya

// initialisasi node js ke project nya dan install package2 yg akan digunakan di project nya
2. npm init 
3. npm install express morgan nodemon pg pg-hstore sequelize 
penjelasan no 3 => pg pg-hstore itu untuk postgresql, sequelize => ORM, morgan => memudahkan logging, express (thirdty package) => memudahkan routing, nodemon => memudahkan saat mau running di console, yg mana dia akan langsung running sendiri ketika disave jadi tidak perlu diulang npm run dari awal lagi

// configurasi npm dan database koneksi
4. configurasi package.json, spt script dev : nodemon index.js, dll.
5. configurasi database.js di folder config untuk database connection dari sequelize nya dengan code => (sequelizerc buat configurasi settingan sequelize-cli)

//data model dan migration ke database menggunakan sequelize
6. sequelize init7. sequelize model:generate --name product --attributes name:string,price:float,stock:integer
contoh=> sequelize model:generate --name transactionDetails --attributes customer_number:string,productId:string,transaction_number:integer,status:
8. sequelize db:create (kalau database yang kita config blm ada)
9.sequelize db:migrate

//Bikin manual folder sesuai mvc
10. buat router 
11. buat controller
12. buat model/ bisa otomatis ada ketika memakai sequelize init